<%= form_with(model: animal, url: local_assigns[:form_url]) do |form| %>
  <% if animal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
        <% animal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div style="margin-top: 20px;">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div style="margin-top: 20px;">
    <%= form.label :additional_hour_fee, style: "display: block" %>
    <%= form.text_field :additional_hour_fee %>
  </div>

  <div style="margin-top: 40px;">
    <%= form.submit style: "background-color:rgb(32, 100, 172); color: white; border: none; padding: 10px 20px; border-radius: 5px; cursor: pointer;" %>
  </div>
<% end %>
